name: Banking API CI pipeline

on:
 push:
  branches:
    -main


jobs:
  first-jobs:
   runs-on: ubuntu-latest

   steps: 

   - name: checkout code
     uses: actions/checkout@v4

   - name: print Welcome Aditya
     run: echo "Welcome Aditya to GitHub Actions World"